<%# a view should loop over these to display them, like in the screenshot below %>
<ul>
  <% @tasks.each do |task| %>
      <li><%= link_to task.title, task_path(task) %></li>
  <% end %>
</ul>

<%= link_to "Add a task", tasks_new_path %>
